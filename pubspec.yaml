name: simplest_logger
description: "A lightweight, opinionated, colorful logging utility for Dart applications. Minimal configuration necessary."
version: 1.0.1
repository: "https://gitlab.com/dart-and-flutter-packages/simplest_logger"

topics:
  - logging
  - logger

environment:
  sdk: ">=3.6.2 <4.0.0"

dependencies:
  logging: ^1.3.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15