/// Defines the available logging levels for controlling log output.
enum SimplestLoggerLevel {
  /// Enables all log messages (info, warning, error).
  all,

  /// Disables all log messages.
  none,
}
